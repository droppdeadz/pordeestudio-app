import { project } from './schemas/project';
import { blogPost } from './schemas/blogPost';
import { siteSettings } from './schemas/siteSettings';

export const schemaTypes = [project, blogPost, siteSettings];
